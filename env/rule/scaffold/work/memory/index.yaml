schema_version: 1
project: change_me
iterations: {}

# Use `python -m hdlflow.cli memory-record ...` to write synchronized memory.
# `iterations` is the canonical mapping keyed by iteration_id.
# Derived views:
# - work/memory/<node>/iterations.md
# - work/memory/active_versions.md
# - work/memory/00_global/CURRENT_STATE.md
