# Change Requests

Create one record per controlled change.

Records are generated by:

```powershell
python -m hdlflow.cli change-open --project <project> --title "..." --reason "..." --scope "..." --risk low
```

Required ID format: `CR-YYYYMMDDHHMMSS-slug`.
