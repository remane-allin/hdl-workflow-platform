# RTL Template Uploads

Place uploaded RTL template packs here before review and normalization.

Reviewed reusable templates should be moved into `lib/sources/rtl_templates/` and indexed in `lib/indexes/template_index.yaml`.
