# RTL Template Uploads

Place uploaded RTL template packs here before review and normalization.

Reviewed reusable templates should be moved into `library/sources/rtl_templates/` and indexed in `library/indexes/template_index.yaml`.
