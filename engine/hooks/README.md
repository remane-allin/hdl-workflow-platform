# Test HDL Hooks

These hooks are scoped to the new `Test/` workflow layout.

They use:

- `config/` for workspace and project configuration
- `projects/<project_name>/memory/` for project memory
- `projects/<project_name>/05_Output/` for canonical editable deliverables
- `.omx/` only as runtime summary state

They do not use the old platform contract.
