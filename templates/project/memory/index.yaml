schema_version: 1
project: change_me
iterations: {}

# Use `python -m hdlflow.cli memory-record ...` to write synchronized memory.
# `iterations` is the canonical mapping keyed by iteration_id.
# Derived views:
# - memory/<node>/iterations.md
# - memory/active_versions.md
# - memory/00_global/CURRENT_STATE.md
