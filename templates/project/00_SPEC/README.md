# 00_SPEC

Only raw source inputs belong here.

- `raw_docs/` - original datasheets, protocol specs, design specs.
- `requirements/` - human-authored functional requirements, acceptance criteria, and design targets.

Do not place parsed, normalized, or generated content in this node.

