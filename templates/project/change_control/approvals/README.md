# Approvals

Record review state before the change is allowed to update deliverables.

Records are generated by `python -m hdlflow.cli change-approve`. Gate runs that
bind `--change-id` require the corresponding request status to be `approved`.
