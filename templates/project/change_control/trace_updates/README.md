# Trace Updates

Record downstream trace matrix updates required by approved changes.

Records are generated by `python -m hdlflow.cli change-close` after gate
evidence exists. This is the handoff point where trace matrices and memory
should match the accepted implementation.
