schema_version: 1
project: Hi3593_v2
status: READY
generated_at: "2026-06-26T20:45:00+08:00"
owner_role: sim
assertions:
  - id: A-TOP-NO-SECOND-RESET
    target: hi3593_v2_top
    intent: "Only MR is exposed as a top-level reset input."
  - id: A-SPI-BYTE-BOUNDARY
    target: spi_slave_if
    intent: "No command side effect is emitted for incomplete SPI bytes."
  - id: A-SPI-CDC-SINGLE-CMD
    target: spi_cmd_cdc
    intent: "Each complete SPI command produces exactly one ACLK command pulse and one SCK acknowledge."
  - id: A-MAILBOX-READ-CLEAR
    target: rx_mailbox_status
    intent: "Mailbox valid pins clear after their read opcode while mailbox payload remains available for readback."
  - id: A-FIFO-BOUNDS
    target: sync_fifo
    intent: "FIFO count stays in 0..32 and honors selected full policy."
  - id: A-TX-NULL-GAP
    target: arinc429_tx
    intent: "TX driver controls return to NULL during reset, selftest, and word gaps."
bind_targets:
  - output/uvm/assertions/hi3593_v2_assertions.sv
disabled_conditions:
  - reset_active
severity_policy:
  - assertion_failure_is_loop2_fail
assumptions: []
